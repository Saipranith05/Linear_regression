creating vertual environment
pythom -m venv venv
activate
convert your woring drectory local reop    git init
git add . to move it to the staging area

first we need to train the model so we divide into 80 and 20% and find it.
next train the model
and next predict the Values of given.